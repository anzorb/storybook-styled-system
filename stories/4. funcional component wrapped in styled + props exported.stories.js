import React from "react";
import El from "../src/components/component wrapped in styled-component exported";

export default {
	title:
		"SC/Functional component wrapped in Styled Component + local prop on the exported component",
	component: El
};

export const basics = () => <El>Hello Button</El>;
