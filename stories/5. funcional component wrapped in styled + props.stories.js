import React from "react";
import El from "../src/components/component wrapped in styled-component";

export default {
	title:
		"SC/Functional component wrapped in Styled Component + local prop on the Styled component",
	component: El
};

export const basics = () => <El>Hello Button</El>;
