import React from "react";
import El from "../src/components/component - styled + func + ss props + prop";

export default {
	title: "Functional Component + styled-system props + local props",
	component: El
};

export const basics = () => <El>Hello Button</El>;
